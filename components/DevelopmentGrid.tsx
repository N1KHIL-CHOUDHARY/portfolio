'use client'

import React from 'react'
import PersonalSection, { PersonalSectionProps } from '@/components/PersonalSection'

export default function DevelopmentGrid(props: PersonalSectionProps) {
  return <PersonalSection {...props} />
}
